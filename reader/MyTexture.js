function MyTexture(id, file_path, amplif_factor) {
     this.id = id;
     this.file_path = file_path;
     this.amplif_factor = amplif_factor;
 };

MyTexture.prototype.applyTexture = function(scene, id, file, factor) {
	// PRECISA DE SER FEITO !!!!!!!!!!!!!!!!
};